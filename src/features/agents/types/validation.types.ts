/**
 * Validation Type Definitions
 * Shared types for form validation across agent features
 */

/**
 * Validation errors for knowledge base creation form
 */
export interface KnowledgeBaseValidationErrors {
  name?: string;
  content?: string;
}
